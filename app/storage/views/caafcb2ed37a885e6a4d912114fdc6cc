<?php echo $__env->make('layouts.dataTables', array_except(get_defined_vars(), array('__data', '__path')))->render(); ?>
<?php $__env->startSection('javascript_bottom'); ?>
<script>
$("#semester_selection").change(function(){
    var projectid = $("select#semester_selection option:selected").val();
   $(location).attr('href',"<?php echo URL::to('/admin/projects/'); ?>/"+projectid);
});
$(document).ready( function () {
    $('#projectListing').DataTable();
});
</script>
<?php $__env->stopSection(); ?>
<?php $__env->startSection('content'); ?>

<div class="form-inline">
    <div class="form-group">`
<select class="form-control" id="semester_selection">
    <?php foreach($semesters as $semester): ?>
    <option value="<?php echo $semester->id; ?>"><?php echo $semester->Name; ?></option>
    <?php endforeach; ?>
</select></div>
        <div class="form-group">
            <a class="btn btn-default" href="/admin/semesters">Semester Manager</a></div>
</div>

<h2 class="sub-header">Projects for <?php echo $activeSemester->Name; ?> <a href="/admin/projects/new" class="btn btn-default">New</a></h2>
<div class="table-responsive">
    <table class="table table-striped" id="projectListing">
        <thead>
            <tr>
                <th>UID</th>
                <th>Name</th>
                <th></th>
            </tr>
        </thead>
        <tfoot>
            <tr>
                <th>UID</th>
                <th>Name</th>
                <th></th>
            </tr>
        </tfoot>
        <tbody>
            <?php foreach($projects as $project): ?>
            <tr>
                <td><?php echo $project->UID; ?></td>
                <td><?php echo $project->Name; ?></td>
                <td>
                    <a class="btn btn-default" href="<?php echo URL::to('/admin/projects/edit/'.$project->id); ?>">Edit</a>
                    <a class="btn btn-default" href="<?php echo URL::to('/admin/projects/delete/'.$project->id); ?>">Delete</a>
                    <a class="btn btn-default" href="<?php echo URL::to('/admin/projects/enroll_users/'.$project->id); ?>">Enroll user</a>
                    <a class="btn btn-default" href="<?php echo URL::to('/admin/projects/overview/'.$project->id); ?>">Overview</a>
                </td>
            </tr>
            <?php endforeach; ?>
        </tbody>
    </table>
</div>
<?php $__env->stopSection(); ?>

<?php echo $__env->make('layouts.master', array_except(get_defined_vars(), array('__data', '__path')))->render(); ?>